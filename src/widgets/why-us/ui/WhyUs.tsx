import { Card } from '@heroui/react'

/**
 * Три блока против трёх тревог из исследования: цена, виза, надёжность.
 * Формулировки не рекламные — каждая называет конкретное действие компании,
 * иначе они не отличаются от текста любого конкурента.
 */
const REASONS = [
  {
    title: 'Виза — наша работа, а не ваша',
    text: 'Собираем пакет документов, записываем на подачу, сопровождаем. Отдельная услуга у визовых центров стоит денег и нервов; здесь она внутри поездки.',
  },
  {
    title: 'Цена известна до оплаты',
    text: 'Менеджер называет итоговую сумму со всеми сборами. Дополнения выбираете сами, и они видны в заявке — доплат «на месте» не появляется.',
  },
  {
    title: 'Отвечаем за поездку целиком',
    text: 'Перенос концерта, задержка рейса, проблема в отеле — решаем мы. При самостоятельной сборке каждый из пяти поставщиков отвечает только за себя.',
  },
]

export function WhyUs() {
  return (
    <ul className="grid grid-cols-1 gap-4 lg:grid-cols-3">
      {REASONS.map((reason) => (
        <li key={reason.title}>
          <Card className="h-full">
            <Card.Header>
              <Card.Title>{reason.title}</Card.Title>
              <Card.Description>{reason.text}</Card.Description>
            </Card.Header>
          </Card>
        </li>
      ))}
    </ul>
  )
}
